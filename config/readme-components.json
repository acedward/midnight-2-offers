{
  "$comment": "Rows of the README's 'Where every component comes from' table. Prose is written here; every pin is a placeholder that scripts/render-readme-pins.py fills from compose/, images/, .env.example and config/artifact-decisions.json — see that script's docstring for the placeholder list. Run `scripts/render-readme-pins.py --write` after editing; ci-check.sh fails when README.md is stale.",
  "columns": [
    { "key": "component", "title": "Component" },
    { "key": "source", "title": "Source" },
    { "key": "pin", "title": "Pin" },
    { "key": "pinnedIn", "title": "Pinned in" }
  ],
  "mustBeEqual": [["KERNEL_REF", "SOLVER_REF"]],
  "rows": [
    {
      "component": "Midnight node `{version:midnight-node}`",
      "source": "[`midnightntwrk/midnight-node`](https://hub.docker.com/r/midnightntwrk/midnight-node) *(upstream image)*, `CFG_PRESET=dev`",
      "pin": "index digest {oci:midnight-node}",
      "pinnedIn": "`config/artifact-decisions.json` · `.env.example`"
    },
    {
      "component": "Node toolkit `{version:midnight-node-toolkit}` (wallet funding)",
      "source": "[`midnightntwrk/midnight-node-toolkit`](https://hub.docker.com/r/midnightntwrk/midnight-node-toolkit) *(upstream image)*, must match the node",
      "pin": "index digest {oci:midnight-node-toolkit}",
      "pinnedIn": "`config/artifact-decisions.json` · `.env.example`"
    },
    {
      "component": "Indexer `{version:indexer-standalone}`",
      "source": "official executable from the [{warehouse}]({warehouse-url}) warehouse, no Rust build; built upstream from {prov:indexer-standalone}",
      "pin": "SHA-256 per arch",
      "pinnedIn": "`config/artifact-decisions.json` · `images/indexer/Dockerfile`"
    },
    {
      "component": "Proof server `{version:proof-server-plain}` ×2 (plain + experimental)",
      "source": "[`ghcr.io/effectstream/midnight-proof-server`](https://github.com/effectstream/midnight-proof-server), a byte-exact mirror of `midnightntwrk/proof-server` (`images/proof-server-mirror/`)",
      "pin": "plain {oci:proof-server-plain} · experimental {oci:proof-server-experimental}",
      "pinnedIn": "`config/artifact-decisions.json` · `images/proof-server-mirror/`"
    },
    {
      "component": "Proof data (SRS K0–K19 + ledger-static)",
      "source": "21 payloads from {warehouse}; initializer from [`acedward/midnight-binary-forge`](https://github.com/acedward/midnight-binary-forge)",
      "pin": "21 SHA-256 · {commit:PROOF_CACHE_SOURCE_COMMIT@acedward/midnight-binary-forge}",
      "pinnedIn": "`config/artifact-decisions.json` · {files:PROOF_CACHE_SOURCE_COMMIT}"
    },
    {
      "component": "Celestia app `{version:celestia-appd}` / node `{version:celestia-node}`",
      "source": "{warehouse}, each archive byte-equal to the official celestiaorg release (`images/celestia/official-equality.tsv`)",
      "pin": "SHA-256 per arch",
      "pinnedIn": "`config/artifact-decisions.json` · {files:CELESTIA_APP_VERSION,CELESTIA_NODE_VERSION}"
    },
    {
      "component": "PostgreSQL 17 + `pg_ivm {full:PG_IVM_VERSION}`",
      "source": "`postgres:17-alpine` *(upstream image)* with `pg_ivm` compiled in; ONE server: db `offerfiles` + db `umbra`",
      "pin": "`PG_IVM_VERSION={full:PG_IVM_VERSION}`",
      "pinnedIn": "{files:PG_IVM_VERSION}"
    },
    {
      "component": "**Offer-files kernel · batcher · COW solver · offer poster · price feed** (ONE image)",
      "source": "[`effectstream/zswap-offerfiles-kernel`](https://github.com/effectstream/zswap-offerfiles-kernel) branch `ledger-v9` ([PR #65](https://github.com/effectstream/zswap-offerfiles-kernel/pull/65)), compactc 0.34.0 · compact-runtime 0.19.0",
      "pin": "{commit:KERNEL_REF@effectstream/zswap-offerfiles-kernel} (`KERNEL_REF` = `SOLVER_REF`)",
      "pinnedIn": "{files:KERNEL_REF,SOLVER_REF}"
    },
    {
      "component": "zswap-da SPA",
      "source": "[`effectstream/effectstream` `templates/zswap-da`](https://github.com/effectstream/effectstream/tree/{full:FRONTEND_REF}/templates/zswap-da) branch **`midnight-1`** ([PR #922](https://github.com/effectstream/effectstream/pull/922) — the local faucet contract removed), fetched at build time and ported from the 1.x line to this stack's 2.x set by `images/zswap-da/ledger-v9.patch`; **no compiler and no contract in the image**",
      "pin": "{commit:FRONTEND_REF@effectstream/effectstream}",
      "pinnedIn": "{files:FRONTEND_REF}"
    },
    {
      "component": "**mint-test-tokens** \u2014 six local test-token issuers + the faucet site",
      "source": "[`effectstream/mint-test-tokens`](https://github.com/effectstream/mint-test-tokens) `main` ([PR #4](https://github.com/effectstream/mint-test-tokens/pull/4)); **NO compiler in the image** \u2014 `contracts/v2/managed/` is tracked upstream and the deploy runner re-proves those bytes against the pinned commit on every run",
      "pin": "{commit:MINT_TEST_TOKENS_REF@effectstream/mint-test-tokens}",
      "pinnedIn": "{files:MINT_TEST_TOKENS_REF}"
    },
    {
      "component": "Shielded NIGHT dApp",
      "source": "[`effectstream/shielded-night`](https://github.com/effectstream/shielded-night) branch `main` — since upstream PR #16 its 2.x profile carries the `undeployed` lane (`MN_ENV=undeployed`, page switch `UNDEPLOYED_PROTOCOL=midnight-2.x`), which retired the `ledger-v9` branch this profile used to track; the v2 contract is recompiled in-image with compactc 0.34.0, byte-identical to the committed `contracts/v2/managed/`",
      "pin": "{commit:SHIELDED_NIGHT_REF@effectstream/shielded-night}",
      "pinnedIn": "{files:SHIELDED_NIGHT_REF}"
    },
    {
      "component": "**AA-v3** — Manager + Minter contracts, relay",
      "source": "[`acedward/AA-midnight-evm-experiment-v3`](https://github.com/acedward/AA-midnight-evm-experiment-v3) `main`; compiled in-image with the kernel's compactc 0.34.0",
      "pin": "{commit:AA_REF@acedward/AA-midnight-evm-experiment-v3}",
      "pinnedIn": "{files:AA_REF}"
    },
    {
      "component": "AA `execute` circuit (MinoCrab, k=18)",
      "source": "[`acedward/AA-midnight-evm-experiment-minocrab`](https://github.com/acedward/AA-midnight-evm-experiment-minocrab) release `{full:MINOCRAB_RELEASE}`; default `AA_ZKIR_SOURCE=minocrab`, unaudited compiler, dev chains only",
      "pin": "{commit:MINOCRAB_REF@acedward/AA-midnight-evm-experiment-minocrab} · `sha256(SHA256SUMS)` {ref:MINOCRAB_SUMS_SHA256}",
      "pinnedIn": "{files:MINOCRAB_REF,MINOCRAB_SUMS_SHA256}"
    },
    {
      "component": "AA web console",
      "source": "this repo",
      "pin": "—",
      "pinnedIn": "`images/aa-contracts/console/`"
    },
    {
      "component": "umbra-evm (read-only eth JSON-RPC)",
      "source": "[`acedward/UmbraDB`](https://github.com/acedward/UmbraDB) branch `evm-compat` ([PR #5](https://github.com/acedward/UmbraDB/pull/5))",
      "pin": "{commit:UMBRA_REF@acedward/UmbraDB}",
      "pinnedIn": "{files:UMBRA_REF}"
    },
    {
      "component": "compactc 0.34.0 (every contract here)",
      "source": "[`midnightntwrk/compact`](https://github.com/midnightntwrk/compact) release, taken by SHA-256",
      "pin": "version + SHA-256",
      "pinnedIn": "`config/artifact-decisions.json` · `images/{aa-contracts,shielded-night,zswap-da}/Dockerfile`"
    },
    {
      "component": "`@effectstream/*` packages",
      "source": "[`effectstream/effectstream`](https://github.com/effectstream/effectstream) on npm: `@effectstream/{celestia,midnight-contracts,orchestrator}@0.200.2` · `mip-zswap-offer@0.4.0-v9.0` · `@midnightntwrk/ledger-v9@1.0.0-rc.3`",
      "pin": "resolved by the kernel lockfile at `KERNEL_REF`",
      "pinnedIn": "—"
    },
    {
      "component": "Web Memo (Memos tab, embedded)",
      "source": "[`acedward/web-memo`](https://github.com/acedward/web-memo) `main` on Cloudflare Pages; builds on [`acedward/midnight-ledger` PR #2](https://github.com/acedward/midnight-ledger/pull/2)",
      "pin": "unpinned (live site)",
      "pinnedIn": "console iframe"
    },
    {
      "component": "Midnight Intents relay (NOT run)",
      "source": "[`shieldedtech/midnight-intents-swaps`](https://github.com/shieldedtech/midnight-intents-swaps) *(upstream)*; the solver observes only, `solver-sink` stands in for the relay's receive half",
      "pin": "`d444c83` via the solver branch",
      "pinnedIn": "—"
    },
    {
      "component": "dusk-wallet (related work)",
      "source": "[`acedward/dusk-wallet`](https://github.com/acedward/dusk-wallet/tree/00001-utxo-pinning) branch `00001-utxo-pinning` · PRIVATE",
      "pin": "—",
      "pinnedIn": "—"
    }
  ]
}
